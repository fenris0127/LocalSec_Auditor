# LocalSec Auditor Frontend

React + Vite + TypeScript frontend for LocalSec Auditor.

## Run

```powershell
npm install
npm run dev
```

The app uses this API base URL by default:

```text
http://127.0.0.1:8000
```

Override it with `VITE_API_BASE_URL` when needed.
